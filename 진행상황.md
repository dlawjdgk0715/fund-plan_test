# 다음에 할 일 (2026-08-25 기준)

> **서식을 손보셨다면** 그 파일을 `templates/서식기준.xlsm` 으로 넣어두세요.
> 다음 달부터 요약1·요약2가 그 서식 그대로 만들어집니다. (templates/README.md 참고)

> 지금까지 무엇이 바뀌었는지는 **[CHANGELOG.md](CHANGELOG.md)** 를 보세요.
> 이 문서는 **앞으로 할 일**만 적습니다.

---

## 지금 상태

M1~M6 구현 완료. 원본 워크북(`카탈리스터_7월_자금운용계획_260714_.xlsm`)과
실제 일일자금계획(`카탈리스터_일일자금계획_2026_7_.xlsx`)으로 계산 검증 통과.
현재 **회사 PC에서 화면을 하나씩 눌러보며 실사용 테스트 중**입니다.

---

## 0. 먼저 해야 하는 것

- ~~템플릿 파일 이름 바꾸기~~ → **해결(8/25).** 이제 이름과 상관없이
  `templates` 폴더에서 확장자가 맞는 가장 최근 파일을 자동으로 찾습니다
- ~~`.gitignore` 에 `*.pptx` 추가~~ → **완료**
- **이 폴더는 아직 git 저장소가 아닙니다** (`.git` 없음).
  8/12 코드 전량 소실 이력을 감안하면 `git init` + Private 원격 연결 권장
- **텐센트클라우드 서비스 이용료 분담(56,328원)** — 계정과목이 `전산유지비`(출금 전용)인데
  방향이 입금이라 수입에 안 잡힙니다. **4번 화면 V4 칸에서 바로 `기타입금` 으로**
  바꿔주세요 (이제 4번 화면 안에서 고칠 수 있습니다)

---

## 1. 바로 이어서 — 실사용 한 바퀴 완주

| 순서 | 화면 | 확인할 것 |
|---|---|---|
| 1 | 1 실적 업로드 | 잔고 자동 인식이 화면에 뜨는지 / 계산값·실제값 대조 결과 |
| 2 | 2 계획 업로드 | 여러 부서 일괄 업로드, 인프라실 시트 선택, 원천세 중복 해소 |
| 3 | 3 실적/계획 관리 | 중복 추적기 · [재계산] 검증 6종 |
| 4 | 4 최종 점검 | 8월 기초잔고가 321.357313으로 들어오는지 · 검증 게이트 전부 초록인지 |
| 5 | 5 엑셀·PPT 만들기 | 엑셀·PPT 생성 후 열어서 숫자 확인 |

### 2026-08-24 코드 점검에서 아직 안 고친 것 (다음 차례)

숫자 오류 4건은 수정 완료(CHANGELOG 참고). 남은 것은 아래 순서로.

**속도** — 8/25에 5번 산출 14.2초 → 1.7초, 1번 화면 엑셀 9번 열기 → 1번으로 정리 완료.
남은 것: 3p(유동자금세부) 쪽에도 "이번 달 값이 0인 행에 템플릿 옛 값이 남는" 문제가 있는지 미확인

**클릭하면 에러나는 것** ← 다음 차례
- 3번 화면: 거래 0건일 때 [저장] → `KeyError: 'id'` / 입금·출금 칸을 비우고 저장하면
  DB에 NULL이 들어가 **그 뒤로 3·4번 화면이 아예 안 열림**
- 2번 화면: 열 자동인식 실패 시 "고칠 게 있어요" 화면이 열리자마자 죽음
- 5번 화면: 산출 파일을 엑셀에서 열어둔 채 다시 누르면 `PermissionError`

**확인 필요 — 데이터 쪽 (코드 문제 아님)**
- 8월 계획의 입금 1건(텐센트클라우드 서비스 이용료 분담 56,328원)이 계정과목
  **'전산유지비'로 분류돼 있는데 입금 방향 매핑이 없어** 수입에 안 잡힙니다.
  → 3번 화면에서 '기타입금' 등으로 고쳐야 자금실적 B.수입에 반영됩니다 (V4 게이트가 잡아줌)

**숫자가 틀릴 수 있는 것 (남은 것)**
- 시트 합칠 때 크로스시트 **범위 참조**가 깨짐 (`=SUM('3p'!G21:G24)` → `G21:G40`)
- 1번 화면 잔고 대조가 **원화와 외화를 섞어서** 합산 → 외화 거래가 있으면 항상 경고
- 환율 0이면 `3p!P4` 는 0, `N월데이터!I1` 은 템플릿 옛 환율이 남아 표마다 환율이 다름
- "전월 마지막 시트" 찾기가 1월인지 확인 안 함 → 7·8월이 든 파일에서 8월을 기초로 읽을 수 있음
- 이번 달 합계가 0인 그룹은 안 덮어써서 **템플릿의 지난달 값이 살아남음**
  (자금실적·연간은 8/25에 해결. 3p 쪽은 아직)
- 엑셀에는 **계획 기초 유동잔고를 넣는 인자 자체가 없음** → PPT와 계획 기말이 달라짐
- 요약1의 검산 잔재(FALSE/TRUE) — 서식기준 파일을 쓰면 그 파일 상태를 따릅니다

**있다고 알고 있었지만 실제로는 동작 안 하던 것**
- 환율 자동조회: 버튼은 있으나 **조회값을 입력칸에 안 넣음**. 조회 기준일도 전월말이 아님
- "지난달과 같은 열 배치 자동 적용": 저장·비교 기준이 어긋나 **한 번도 재사용된 적 없음**
- 계획 파일을 고쳐서 다시 올려도 **이전 내용이 그대로 표시**(캐시 키 버그)
- 검증 게이트 **V3·V6은 구조상 절대 실패할 수 없는 항등식**
- 4번에서 고친 환율·계획 기초잔고가 **5번에 반영 안 됨**
  (8/25: 4번 입력값이 페이지 이동 후에도 남도록은 고쳤으나, 5번으로 넘기는 것은 아직)

---

### 2026-08-25 개선요청 처리 결과

Home / 1 실적 업로드 / 4 최종 점검 / 5 엑셀·PPT 만들기 — **전부 반영 완료** (CHANGELOG 5차).
남은 논의거리 두 가지:

1. **양방향 거래를 계정과목 사전에서 어떻게 다룰까** — 텐센트클라우드 건처럼 같은
   계정과목이 입금·출금 양쪽으로 발생하는 경우. 지금은 매달 손으로 고쳐야 합니다.
   계정과목별 '양방향 허용' 플래그를 두고 입금 시 들어갈 행을 따로 지정하는 방식 검토
2. **비고 서식 학습 결과 확인 화면** — 지금은 조용히 배우기만 합니다.
   6번 '비고 라벨 관리' 화면에 "지금 배운 서식" 을 보여주고 되돌릴 수 있게 하면 좋겠습니다

### ⚠️ 아직 한 번도 확인 못 한 것
- **환율 API 방화벽 테스트** — 5번 화면 "환율 연결 테스트" 버튼
  (클라우드 환경에서는 외부 접속이 막혀 있어 테스트 불가였음)
- **외화(USD) 거래가 있는 달**의 환산 계산 — 6·7월에 사례가 없었음
- **연말/연초** — 1월 파일에서 12월 시트를 기초잔고로 잘 찾는지

---

## 2. 미구현 마일스톤

- **M7** 확정 버튼 · 버전 메모 · 스냅샷 · 롤오버
  (PPT 자동 생성은 **완료** — 6장 세부내역 표만 수동)
- **M8** 구글드라이브 동시 접근 안전장치
- **M9** 화면 디자인 정리

---

## 3. 남은 자잘한 것

- **자산매각(차량 매각) 행** — 유동자금세부·자금실적 양쪽에 대응 행 없음.
  실제 매각이 생기면 `tools/add_marketing_row.py` 와 같은 방식으로 추가
- **배치 실행 파일(.bat)** — 만들었으나 실행 오류. 나중에 다시 시도
- **태그 사전 채우기** — 7번 화면에서 부서 파일 표현을 미리 등록해두면
  첫 달부터 자동 분류율이 크게 올라감
- 깃허브 저장소는 **Private** 필수. `db/`·`templates/` 는 .gitignore 처리됨

---

## 4. 개발할 때 기억할 것

- `core/` 파일을 바꾸면 **cmd를 껐다 켜야** 반영됨 (F5로는 안 됨)
- 실행: `python -m streamlit run app/Home.py`
- **파일 구조를 추측하지 말 것.** 지금까지 설계서와 실제 파일이 계속 달랐음
  (계정과목 이름, 숨김 A열, 표가 두 개, 합계 열, 숨김 시트 72개 …)
  새 양식을 다룰 때는 **실물 파일을 먼저 열어볼 것**

---

## 5. 다음 세션 시작 방법

이 파일과 `CHANGELOG.md` 를 올리고 **"자금운용계획 자동화 이어서 하자"** 라고 하시면 됩니다.
원본 워크북과 일일자금계획 파일도 같이 주시면 검증까지 바로 가능합니다.
