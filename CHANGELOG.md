# 변경 이력

버전은 날짜 기준입니다. 최신이 맨 위.

---

## 2026-08-25 (14차) — 시작 화면: 작업 순서 → 진행 현황

**'작업 순서 — 눌러서 이동하세요' 를 '진행 현황' 으로 교체.**
어디까지 했는지, 지금 뭘 해야 하는지가 한눈에 보입니다.

| | 내용 |
|---|---|
| 상태바 | `완료한 단계 2 / 4` + 진행 막대 |
| 완료 칸 | 회색 — 요약만 (`29건 · 시트 4개`) |
| 남은 칸 | **옅은 빨강 + 깜빡임** (`계정과목 확인 필요 1건`) |
| 이동 | 단계 이름이 곧 링크 — 별도 [이어서 하기] 버튼 없음 |

**사용자가 체크할 필요 없습니다** — 이미 저장된 자료에서 매번 계산합니다.
따로 로그를 남기면 실제 자료와 어긋날 수 있어서 계산으로만 알아냅니다.

| 단계 | 완료 조건 | 남았을 때 보여주는 것 |
|---|---|---|
| 1 실적 업로드 | 실적 ≥ 1건 | "일일자금계획 파일을 올려주세요" |
| 2 계획 업로드 | 계획 ≥ 1건 | **들어온 부서 이름** — 누가 안 왔는지 판단용 |
| 3 실적/계획 관리 | 확인 필요 0건 | `계정과목 확인 필요 N건` |
| 4 최종 점검·산출 | 빨간 게이트 0 + 최신 산출물 | `확인할 검증 N개 (V4)` |

**같이 고친 것**
- 계정과목이 **비어 있는** 거래는 `aggregate` 가 조용히 건너뛰어 3단계 판정에서
  안 잡혔습니다 → 따로 세도록 수정
- '이미 저장된 자료' 안내 줄은 진행 현황과 겹쳐서 삭제

`core/progress.py` 신설 (판정 로직, 화면과 분리). 시작 화면 로드 **0.20초** 유지.
검증: `tests/test_ui.py` 5항목 추가 (전체 103항목 통과).

---

## 2026-08-25 (13차) — 화면 정리: 5번 삭제, 시작 화면에서 바로 받기

**5번 'Export Excel' 삭제.** 4번이 이미 다 하고 있었습니다(수치 표·비고 확인·받기).
"파일만 받고 싶은 사람"은 **시작 화면**에서 바로 받게 했습니다.

| | before | after |
|---|---|---|
| 사이드바 | 7개 | **4개** (+ 가끔 쓰는 2개는 토글로) |
| 파일만 받기 | 5번 페이지 | **시작 화면 `📥 만들어 둔 파일 받기`** |
| 산출 | 4번 + 5번 (중복) | 4번 `③ 엑셀·PPT 받기` |

**시작 화면 받기 칸**
- 마지막으로 만든 파일 + 만든 시각 표시, 버튼 눌러 바로 다운로드
- 그 뒤로 내용이 바뀌었으면 "다시 만들어주세요" 경고
- 아직 만든 게 없으면 4번으로 안내

**가끔 쓰는 화면 감추기** (`core/nav.py`)
- 6 비고 라벨 관리 · 7 태그 사전 관리는 평소 사이드바에서 숨김
- 시작 화면의 **[가끔 쓰는 화면도 보기]** 스위치로 켜고 끄기 (껐다 켜도 기억)

**5번에만 있던 것 처리**

| 항목 | 처리 |
|---|---|
| 실적 월 / 계획 월 직접 지정 | 삭제 (실적월에서 자동) |
| 환율 연결 테스트 | 삭제 (아직 동작 안 하는 미완성 기능) |
| PPT 템플릿 파일명 안내 | 4번 받기 칸으로 |

검증: 전체 98항목 통과. 시작 화면에서 실제로 받아지는지, 페이지가 4(+2)개로
정리됐는지 확인.

---

## 2026-08-25 (12차) — 비고만 고치고 바로 받기 (재사용성)

**사용 패턴**: 실적은 거의 안 고치고, 계획은 가끔, **비고를 가장 많이** 고침.
그런데 비고 하나 고치는데 화면을 옮겨야 파일이 나왔습니다.

| | before | after |
|---|---|---|
| 클릭 수 | 8회 | **5회** |
| 화면 이동 | 2회 | **0회** (4번에서 바로) |
| 같은 내용 재생성 | 매번 3.3초 | **0.0초** (지난 파일 재사용) |

**바뀐 것**

- **4번 화면 맨 아래에 `③ 엑셀·PPT 받기`** 추가 — 비고 저장 → 바로 만들고 받기
- **바뀐 게 없으면 안 만듭니다.** 거래·비고·잔고·환율·템플릿을 하나의 지문으로 묶어
  두고, 그대로면 지난 파일을 즉시 드립니다. 바뀌면 "내용이 바뀌었습니다" 표시
- **[🔄 새로 만들기]** 버튼 — 앱이 모르는 변경(서식기준 파일 교체 등)에 대비
- 다운로드는 그대로 **엑셀·PPT 버튼 2개**, 저장 위치도 `data/output` 그대로

**코드 정리**

- `core/outputs.py` 신설 — 산출물 만들기·지문·재사용을 한 곳에
- `core/output_ui.py` 신설 — 만들기/받기 칸을 4·5번이 **같은 코드**로 그림
- 5번 화면은 얇아졌습니다 (수치 확인 표 + 받기 칸)

검증: `tests/test_ui.py` 5항목 추가 (전체 100항목 통과).
재사용 0.00초, 비고·환율 변경 시 재생성, 4번에서 바로 받기까지 실제로 눌러서 확인.

---

## 2026-08-25 (11차) — 비고 저장 누락 · 비운 줄 · 계정과목 교정

| # | 증상 | 원인 | 조치 |
|---|---|---|---|
| 1 | 세부내역 비고를 고쳐도 저장 안 됨 (36줄 중 1줄만 저장) | 편집표가 form 밖 → 칸에 쓰고 **바로 [저장]** 누르면 그 값이 서버로 안 넘어감 | 편집표를 `st.form` 으로 묶어 [저장] 시 표 전체를 한 번에 전송 |
| 2 | **비워둔 줄에 초안이 되살아남** | `저장 안 함` 과 `일부러 비움` 을 둘 다 `""` 로 취급 | 세 가지 상태로 구분 (`load_report_remarks_raw`) |
| 3 | 삼성카드 법인카드 대금이 기타지출 | 원본 파일의 계정과목이 그렇게 적혀 옴 | 업로드 시 **자동 교정** + 교정 내역 화면 표시 |

**비고 상태 3가지**

| DB 값 | 뜻 | 산출물 |
|---|---|---|
| 없음 | 아직 안 건드림 | 앱 초안 |
| `""` | 일부러 비움 | **빈칸** |
| 글자 | 직접 쓴 글 | 그 글 |

- 화면 아래 `💾 저장 상태 — 직접 쓰신 글 N줄 · 일부러 비우신 줄 N줄 · 안 건드린 초안 N줄`
- **[↩ 초안으로 되돌리기]** = 저장 기록 삭제 → 초안 복원 (`clear_report_remarks`)

**계정과목 자동 교정 규칙** (`core/tag_classifier.RETAG_RULES`)

| 적요 | 거래처 | 원본 → 교정 |
|---|---|---|
| `법인카드` | `카드` 포함 | 기타지출 → 직원비용 |

거래처 조건이 없으면 '법인카드데이타 연동서비스'(소프트웨어) 같은 건까지 잡혀서 범위를 좁혔습니다.

검증: `tests/test_ui.py` 4항목 추가 (전체 90항목 통과).

---

## 2026-08-25 (10차) — cmd 재시작 후 다시 안 올려도 됨

**증상**: 새 버전 받을 때마다 실적·계획 파일을 매번 다시 올려야 했음.

**원인**: 거래 내역은 DB에 남지만, **세션(st.session_state)** 은 cmd 창을 닫으면
사라집니다. 업로드 파일 기록도 세션에만 있어서 재시작하면 "파일을 올려주세요"로 돌아갔습니다.

| 무엇 | 재시작하면 | 조치 |
|---|---|---|
| 거래·잔고·비고·태그 (DB) | **남음** | — |
| 올린 파일 기록 | 사라짐 | `data/_state/*.json` 에 기록 → 되살림 |
| 실적 월 | 초기화 | 같은 방식으로 기억 |
| 입력칸·화면 상태 | 사라짐 | 그대로 (기본값 재계산) |

- Home 에 **"💾 이미 저장된 자료 — 실적 N건 / 계획 N건"** 표시. 3번부터 바로 갈 수 있음을 안내
- 파일을 바꾸려면 새로 올리거나 **[↺ 파일 비우기]**
- 보관 파일은 종류별 최근 8개까지만 유지 (`data/_uploads`)

검증: `tests/test_ui.py` 3항목 추가 (전체 85항목 통과). 세션이 완전히 빈 상태로
화면을 띄워 파일·실적월이 되살아나는지 확인.

---

## 2026-08-25 (9차) — 비고 서식 · 표 이름 · 출력물 연결

| # | 증상 | 원인 · 조치 |
|---|---|---|
| 1 | 미리보기 탭이 '1. **7**월 자금수지' | 표 이름은 **보고 대상 월(계획월)** 기준. 원본 PPT도 '7월 자금운용계획' 안에 '1. 7월 자금수지' → `1. {p}월 자금수지` 로 |
| 2 | 비고에 ● / ○ 기호가 없음 | 저장할 때 **자동으로 붙입니다** (● 실적월 / ○ 계획월). 기초·기말잔고 문장도 원본 서식 그대로 자동 작성 |
| 3 | PPT 2장 비고 5행에 **지난달 문장이 남음** | 실적·계획을 한 칸에 몰아넣고 아랫줄(3·5행)은 손대지 않았습니다 → 원본처럼 **● 윗줄 / ○ 아랫줄** 로 나눠 쓰고 빈 줄은 지웁니다 |
| 4 | 세부내역 비고에 월이 안 보이는 줄이 있음 | 출력물이 **'저장한 줄'만** 썼습니다. 화면엔 초안이 보이는데 PPT·엑셀엔 빈칸 → 이제 **화면과 같은 값**(고친 글 없으면 초안)을 씁니다 |
| 5 | 4번에서 고친 요약1이 엑셀에 반영 안 됨 | 연결은 정상이었습니다(실측 확인). 다만 **7차 이전에 '자금수지' 표에 직접 써 두신 글**은 요약1로 안 넘어갔습니다 → 그 글도 자동으로 이어받습니다 |

**덤**: 사용자가 월 표기 없는 문장을 한 번 쓰면 그 뒤 모든 초안에서 `∙7월 :` 이 통째로
사라지던 문제를 막았습니다. 서식 학습은 금액 단위·자릿수·개수·구분기호만 배우고,
**월 표기는 항상 유지**합니다.

**비고 한 곳 정리** — 4번 '1. 자금수지' 탭에서 **여섯 문장**(기초잔고 / ● 실적 수입 /
○ 계획 수입 / ● 실적 지출 / ○ 계획 지출 / 기말잔고)만 손보면
엑셀 요약1·PPT 2장이 같은 문장을 씁니다.

검증: `tests/test_ui.py` 6항목 추가 (전체 82항목 통과). 받으신 PPT 파일로 직접 대조.

---

## 2026-08-25 (8차) — 3번 화면 수기 입력 안내창

취합 파일에 안 나오는 항목이 이번 달에 하나도 없으면 **안내창(팝업)** 을 띄웁니다.

| 항목 | 찾는 조건 | 기본 계정과목 |
|---|---|---|
| 주택자금대출 이자 지원 | 적요에 `주택자금` | 복리후생비 (출금) |
| 삼성카드 법인카드 대금 | 거래처 `삼성카드` **＋** 적요에 `법인카드` | 직원비용 (출금) |

- 창 안에서 **거래처·적요·계정과목·금액을 넣고 [➕ 이 건 추가]** 하면 바로 저장됩니다
- 해당 없는 달이면 **[이번 달은 없음 — 닫기]**. 창은 닫혀도 위쪽 경고 줄은 남고,
  [🖐 안내창 다시 보기] 로 다시 열 수 있습니다
- 해당 거래가 생기면 경고·안내창이 자동으로 사라집니다

**같이 고친 것**
- 예전 규칙은 **계정과목까지 맞아야** 찾은 것으로 쳐서, 계정과목이 아직 안 붙은 거래를
  못 알아보고 **있는데도 없다고** 했습니다 → 이제 낱말(+거래처)로만 찾습니다
- 거래처 조건(`counterpart`)·안내 문구·기본 계정과목 열을 `recheck_items` 에 추가
- 기본 규칙이 `db/seed_tags.py` 와 `core/data_store.py` 두 곳에 있어 **규칙이 두 벌**
  생기던 문제 정리 — 이제 `RECHECK_DEFAULTS` 한 곳만 씁니다 (옛 중복 행은 자동 삭제)

검증: `tests/test_ui.py` 7항목 추가 (전체 76항목 통과).

---

## 2026-08-25 (7차) — 기초 운용자금 · 비고 확정 위치 · PPT 출력 6건

### 기초 운용자금도 일일자금계획에서 읽습니다
전에는 '지난달에 앱으로 산출한 이력'에서만 가져와서, 첫 달이나 이력이 없으면 0이었습니다.
이제 **전월 마지막 시트(예: 0630-1)의 「3. 자금현황 > 3) 운용자금」** 을 그대로 읽습니다
(`month_balances.open_invest_krw` 신설 · 기존 DB에는 `_ensure_columns` 로 열 추가).
찾는 순서는 ① 시트에서 읽은 값 → ② 지난달 산출 이력 → ③ 지난달 당월말 값 (`resolve_open_invest`).
※ 이미 쓰시던 DB에는 이 값이 없으니, **1 실적 업로드를 한 번 다시** 해주세요.

### 비고는 4번에서만 확정합니다
5번 화면의 '요약1 서술형 비고 초안' 칸(제목도 길었음)을 없앴습니다.
이제 **4 최종 점검 → '1. 자금수지' 탭**에서 네 문장만 고치면
엑셀 요약1(I8~I11)과 PPT 2장 표에 **같은 문장**이 들어갑니다.
5번에는 '비고 확인' 접이식 칸만 남아, 무엇이 들어가는지 눈으로 확인만 합니다
(요약1 4줄 + 세부내역 몇 줄이 반영되는지 목록으로 표시).

세부내역 탭에는 **'엑셀' 표시(○/—)** 를 넣었습니다. 굵은 소계 줄(수입·지출·인건비·기타)의
비고는 엑셀 유동자금세부 N열에 대응 행이 없어 **PPT에만** 들어갑니다 — 이제 화면에서 구분됩니다.

### 🔴 비고를 지우면 'nan' 이 찍히던 문제
편집표에서 칸을 **지우면** 판다스가 빈 문자열이 아니라 `NaN`(숫자 결측값)을 돌려줍니다.
`str(NaN)` 은 `"nan"` 이라 그 글자가 엑셀·PPT에 그대로 나갔습니다.
`clean_text()` 를 저장·불러오기·엑셀 쓰기·PPT 쓰기 네 군데 모두에 넣어 공란으로 만듭니다.
(예전에 저장돼 버린 "nan" 도 불러올 때 걸러집니다)

### PPT 출력물 6건
| # | 증상 | 원인 · 조치 |
|---|---|---|
| 1 | 2장 '2. 유동자금 계획' 표에 **7월 계획** | 월 치환이 **글상자만** 훑고 표 안은 안 봤습니다 → 표 셀까지 훑도록 |
| 2 | 3장 '3. 운용자금 계획' 표에 6월 실적/7월 계획 | 위와 같은 원인 · 같은 조치 |
| 3 | 5장 연간자금실적(2026) 7·8월 미반영 | B.수입·C.지출 두 줄만 채우고 있었습니다 → **기초·수입(경상/비경상)·지출(경상/비경상)·인건비·S/W,H/W·기타 세부·수지차·경상수지차·조달·기말** 을 실적월·계획월 두 칸 모두 채우고 **합계·월평균도 다시 계산** |
| 4 | 비고 고치면 서식이 깨짐 | 줄바꿈이 든 글을 run 하나에 통째로 넣어 줄이 안 나뉘었습니다 → **문단으로 분리** |
| 5 | 비고 지우면 nan | 위 `clean_text` |
| 6 | 6장 비고 글자 크기 제각각 | 비고 칸 전체를 **맑은 고딕 8pt** 로 통일 (`_set_note`) |

덤으로, 템플릿이 몇 월 파일인지 **자동으로 알아냅니다**(`detect_template_months`).
전에는 '6월 실적·7월 계획'으로 코드에 박아 둬서, 템플릿을 다른 달 파일로 바꾸면
월 표기가 하나도 안 바뀌었습니다.

검증: `tests/test_ui.py` 11항목 추가 (전체 69항목 통과).
PPT는 실제로 만들어 표 머리글·연간표 숫자·비고 글꼴(8pt 맑은 고딕)·빈칸을 직접 확인하고,
엑셀은 LibreOffice 재계산으로 오류 0건 · 'nan' 0건을 확인합니다.

---

## 2026-08-25 (6차) — 올린 파일이 화면을 옮기면 사라지던 문제

**증상**: 1번에서 파일을 올린 뒤 2번에 갔다 돌아오면 "파일을 올려주세요" 로 되돌아감.

**원인**: 코드 오류가 아니라 **스트림릿의 원래 동작**입니다.
화면(페이지)을 옮기면 그 화면의 위젯 상태를 지우는데, `st.file_uploader` 도 예외가 아닙니다.

**조치** (`core/uploads.py` 신설)
- 올린 파일을 `data/_uploads/` 에 보관하고, 그 목록만 세션에 기억합니다.
  세션 값은 화면을 옮겨도 남으므로 돌아오면 **그 파일을 그대로 이어서** 씁니다.
  화면에는 "직전에 올리신 ○○ 을 그대로 씁니다" + [↺ 파일 비우기] 가 뜹니다.
- 1번(일일자금계획)·2번(부서 계획서 여러 개) 양쪽에 적용했습니다.

**덤으로 고친 잠재 버그**: 예전에는 늘 같은 이름(`_upload_actual.xlsx`,
`_plan_{부서}.xlsx`)에 덮어썼습니다. 그래서 **A 올림 → B 올림 → 다시 A 올림** 순서에서
마지막 A 는 캐시에 걸려 저장을 건너뛰는데 디스크에는 B 가 남아, **엉뚱한 파일을 읽을**
수 있었습니다. 이제 보관 파일 이름에 **내용 지문(sha1)** 을 넣어 이름과 내용이 항상
짝을 이룹니다. 종류별로 최근 8개만 남기고 오래된 것은 자동으로 지웁니다.

검증: `tests/test_ui.py` 3항목 추가 (전체 58항목 통과).
'2번 갔다 돌아온 상태'를 AppTest 로 재현해 다시 안 올려도 결과가 바로 나오는지,
보관 파일이 지워지면 처음 상태로 돌아가는지, A→B→A 에서 내용이 안 섞이는지 확인합니다.

---

## 2026-08-25 (5차) — 화면 사용성 개선 (개선요청사항_260825)

### 1. Home
- "화면이 안 보이거나 오류가 날 때" 안내를 **2단계 순번**으로 축약
  (① 폴더에서 cmd 열기 → ② `python -m streamlit run app/Home.py`,
   팀원은 뒤에 `--server.address 0.0.0.0`)
- **"발견된 화면 파일" 목록 삭제** — 개발 중 진단용이라 사용자에게 의미 없음
- 메뉴 이름: `4 전체 미리보기` → **4 최종 점검**, `5 엑셀 산출` → **5 엑셀·PPT 만들기**

### 2. 실적 업로드
- 시트별 결과표를 **시트명 / 실적 / 계획 / 합계 / 계정과목 있는 항목(계)** 5열로,
  숫자 뒤에 "건" 표시
- "시트별로 어떤 표를 찾았는지 자세히 보기"(헤더행·구간·열 — 개발자용) 를
  **원본 소계 대조표**로 교체.
  일일자금계획 시트가 **직접 적어 둔 '소계' 금액**과 앱이 읽은 합계를 나란히 놓아,
  빠뜨린 줄이 있는지 사람이 눈으로 바로 검증할 수 있습니다 (`daily_sheet_subtotals`)
- "계정과목이 비어 있는 거래" → **계정과목 미기재 항목**,
  "비어 있는 N건 전체 보기" → **항목 N건 상세 보기**
- 저장 후 안내는 **"다음은 2 계획 업로드"** 한 줄로. 재계산 안내는 3번 화면으로 이동

### 4. 최종 점검 (옛 '전체 미리보기') — 화면 재구성
`① 수치 확인` → `② 보고서 미리보기` 로 위·아래 배치.

- **총자금 섹션을 위로** 이동. 유동자금·운용자금·총자금을 실적월/계획월 6칸으로 표시
- **검증 게이트**: 통과 = 초록 "특이사항 없음" / 문제 = 빨강.
  빨강 칸의 [확인 / 수정] 을 열면
  · V1·V4 는 **문제 거래를 이 화면에서 바로 고칩니다**(계정과목 선택·집계 제외).
    같은 DB를 쓰므로 3·5번 화면에 즉시 반영됩니다
  · [✅ 확인함] 을 누르면 초록으로 바뀌고, **그 시점의 거래 지문(sha1)** 을 함께 저장합니다.
    나중에 거래가 바뀌면 지문이 달라져 **확인이 자동으로 풀리고 다시 빨간불** 이 됩니다
    (`gate_acks` 테이블 · `txn_fingerprint`)
- **기초 유동잔고 입력값이 초기화되던 문제 해결** — number_input 에 key 를 붙이고
  기본값을 처음 한 번만 넣습니다. 화면을 옮겼다 돌아와도 값이 남습니다
- 외부 정합성(V2) 금액 표시를 **소수점 둘째 자리**까지로 (판정은 6자리 그대로)
- 세부내역 표의 **`b-a` 열 삭제** (헤더 칸 수가 안 맞아 비고 오른쪽으로 밀려 나오던 것).
  PPT·엑셀에는 그대로 들어갑니다

### 4. 비고 — 표에서 직접 수정 + 서식 학습
- 숨김 메뉴(expander) 를 없애고, 표 아래 **편집표에서 바로 고치고 [비고 저장]**.
  여러 번 고칠 수 있고, [↩ 초안으로 되돌리기] 도 있습니다
- **자금수지 표**와 **유동자금 계획 세부내역 표** 비고는 앱이 과거 PPT 서식대로
  초안을 미리 채웁니다 (계정과목별 큰 금액 순 최대 3건)
- 사용자가 고치면 **'쓰는 방식'만 학습**합니다 (`core/remark_style.py`).
  금액 단위(백만/억)·소수 자릿수·항목 개수·구분 기호·줄 앞머리·"등" 사용 여부.
  **문장 내용은 배우지 않습니다** — 매달 새 거래로 다시 계산하므로 금액이 틀릴 일이 없습니다
- 4번에서 고친 세부내역 비고는 **엑셀 유동자금세부 N열에도 그대로** 들어갑니다

### 5. 엑셀·PPT 만들기
- 제목 **"엑셀·PPT 만들기"**, "사람이 직접 넣던 값" → **"최종 수정"**,
  "무엇을 만들까요?" → **"보고서 생성"** (글자 키움, 엑셀·PPT 중복 선택 가능)
- **템플릿을 파일 이름과 상관없이 찾습니다** (`core/config.find_template`).
  정해진 이름이 없으면 `templates` 폴더에서 확장자가 맞는 **가장 최근 파일**을 씁니다
  (`_fast_…` 속도용 사본, `~$…` 엑셀 임시파일, `서식기준.xlsm` 은 제외).
  → PPT 옵션이 비활성화되던 문제의 근본 원인 제거

### 검증
`tests/test_ui.py` 14항목 신규 (전체 55항목 통과).
스트림릿 **AppTest 로 화면 4개를 실제로 그려** 오류 0건을 확인하고,
[확인함] 클릭 → 초록, 거래 수정 → 다시 빨강 까지 눌러서 검증합니다.
※ 테스트는 임시 DB를 쓰므로 실제 업무 DB(`db/tags.db`)를 건드리지 않습니다.
엑셀 산출물 LibreOffice 재계산 오류 0건.

### 사용자가 직접 하실 일
- **텐센트클라우드 서비스 이용료 분담(56,328원)** — 계정과목이 `전산유지비`(출금 전용)인데
  방향이 입금이라 수입에 안 잡힙니다. 4번 화면 V4 칸에서 `기타입금` 으로 바꿔주세요.
  (양방향 거래를 계정과목 사전에서 어떻게 다룰지는 별도 논의 필요)

---
## 2026-08-25 (4차) — 잔고 불일치 3건 + 서식 유지

### 🔴 연간·자금실적의 기말잔고가 요약1과 달랐던 이유 — 원인 셋
| # | 원인 | 크기 |
|---|---|---|
| A | **기초잔고를 안 맞춤** — 연간·자금실적·요약1 실적월 기초가 템플릿에 쌓여 온 값 그대로였음 | 0.009 ~ 26백만 |
| B | **금액이 0인 그룹을 안 덮어씀** — 템플릿(지난달 파일)의 값이 그대로 살아남아 지출이 부풀려짐 (식당·카페 0.097 / 지급수수료 0.3 등) | 7월 +0.097 / 8월 +0.609 |
| C | **원본 3p 수식 자체의 결함** — 아래 참고 | 8월 +0.684 |

**C 상세**: 원본 워크북의 SUMIFS 기준이 행마다 제각각이었습니다.
- `J29`(전산관련지출 **계획**)는 기준을 **라벨 셀 `F29`**("전산관련지출")로 잡는데
  데이터의 계정과목은 `"전산유지비"` → **늘 0**. `H29`·`K29`도 같은 문제
- `G27`(투자금 실적)은 `지분투자`를 더하는데 `J27`(계획)은 안 더함

→ 산출할 때 3p의 집계 행 수식(G·H·J·K)을 **태그 사전 기준으로 통일해서 다시 써넣습니다**
  (이번 데이터 기준 83칸 교체). 이제 요약1·PPT·미리보기·연간·자금실적이 전부 같은 기준입니다.

**조치 후** — 세 시트가 정확히 일치합니다.
```
요약1     기말  7월 2,421.357313   8월 2,390.3671738
연간      기말  7월 2,421.357313   8월 2,390.3671738
자금실적  기말  7월 2,421.357313   8월 2,390.3671738
```
LibreOffice 재계산 오류 0건. 3p 전 항목이 계산 엔진 값과 **소수점까지 일치**함을 확인.

### 손보신 서식을 계속 유지하는 방법 — `templates/서식기준.xlsm`
산출물의 열 너비·표 위치·글꼴을 직접 다듬으셨다면 그 파일을 이 이름으로 넣어두세요.
다음 달부터 **요약1·요약2를 새로 조립하지 않고 그 파일의 시트를 그대로 가져다 씁니다.**
두 시트는 거의 전부 수식이라 숫자는 알아서 새로 계산됩니다.

앱이 갈아 끼우는 것은 세 가지뿐입니다.
| 갈아 끼우는 것 | 찾는 방법 |
|---|---|
| 월 표기 | 머리글 `N월(a)` / `M월(b)` 를 읽어 자동 판단 |
| 기초잔고 | '기초잔고'·'기초자금' 글자를 찾아 오른쪽 칸에 |
| 비고 4줄 | '비고' 열 + ' B. 수입'·' C. 지출' 행 |

나머지 시트의 열 너비·행 높이도 이 파일을 따릅니다. 파일이 없으면 기존 방식대로 조립합니다.

### 회귀 테스트
- 3p 집계 수식이 태그 사전과 정확히 일치하는지 (실적·계획 양쪽 전 항목)
- 서식기준 파일의 열 너비·병합이 산출물에 그대로 유지되는지
- → `tests/test_report.py` **총 19항목**

---

## 2026-08-25 (3차) — 속도

### 측정 결과 (원인 진단)
| 무엇 | 전 | 후 | 원인 |
|---|---|---|---|
| **5 엑셀 만들기** | **14.2초** | **1.7초** | 템플릿 여는 데만 10초 |
| **1 실적 업로드** (클릭 1번마다) | 엑셀 **9번** 열기 | **1번** | 캐시 없음 |
| 2 계획 업로드 (5.1MB·76시트) | 0.19초 | 0.19초 | 이미 최적 |
| 화면 이동 (DB·집계) | 0.005초 | 0.005초 | 문제 없음 |

### 🔴 가장 큰 범인은 코드가 아니라 **원본 템플릿 파일**
- `xl/styles.xml` 이 **15.9MB**, 셀 스타일이 **60,316개**(실제 사용 1,727개).
  엑셀에서 오래 쓰면 안 쓰는 셀 스타일이 쌓이는 흔한 현상입니다.
- 대응: 안 쓰는 스타일만 걷어낸 **가벼운 사본**(`templates/_fast_…xlsm`)을
  처음 한 번 만들어 두고 그걸로 산출합니다. 원본은 건드리지 않습니다.
  원본이 바뀌면(크기·수정시각) 자동으로 다시 만듭니다.

  | | 원본 | 정리본 |
  |---|---|---|
  | 파일 크기 | 2,522 KB | **391 KB** |
  | 여는 시간 | 9.8초 | **0.9초** |
  | **산출물 크기** | — | **120 KB** (엑셀에서 열 때도 빨라짐) |

- **검증**: 원본으로 만든 산출물과 정리본으로 만든 산출물을 **전 시트 셀 단위 비교**
  → 값·숫자서식·글꼴·채우기·테두리·병합·열너비 **차이 0건**.
  LibreOffice 재계산 오류 0건, 기말 `321.357313` 재현
- 정리에 실패하면 조용히 원본을 그대로 씁니다(경고 표시)

### 1 실적 업로드 — 클릭할 때마다 다시 읽던 것 정리
- 스트림릿은 화면에서 뭘 누를 때마다 코드를 처음부터 다시 실행합니다.
  1번 화면은 캐시가 없어서 그때마다 **업로드 파일을 디스크에 다시 쓰고
  엑셀을 9번 다시 열었습니다.**
- `core/parsers.py`에 **`daily_bundle()`** 신설 — 파일을 **한 번만** 열어
  시트 목록·시트별 내용·전월 마지막 시트를 한꺼번에 가져옵니다
- 업로드 파일은 **내용이 바뀌었을 때만** 디스크에 저장(내용 지문 비교)
- 읽기·파싱 결과를 캐시 → 체크박스·드롭다운을 눌러도 다시 안 읽습니다

### 🔴 2 계획 업로드 — 캐시가 사실상 꺼져 있던 버그
- `@st.cache_data` 는 **밑줄로 시작하는 인자를 해싱하지 않습니다.**
  파일 지문을 `_stamp` 로 넘기고 있어서 캐시 키에서 빠졌고,
  **같은 부서 파일을 고쳐서 다시 올려도 이전 내용이 그대로** 나왔습니다
- `_stamp` → `stamp` 로 수정
- 회귀 테스트 추가: **캐시 함수 인자에 밑줄이 없는지 전 화면 정적 검사**

### 회귀 테스트
- `daily_bundle` 이 파일을 1번만 여는지 검사 + 위 정적 검사 → **총 18항목**

---

## 2026-08-25 (2차)

### 🔴 시트 이름 변경이 파일 전체를 깨뜨리던 문제 수정
- 증상: `6월데이터`→`실적월` 로 바꾸자 **유동자금세부가 전부 `#VALUE!`**
- 원인: 이름 바꾸기 로직(`_patch_cross_sheet_refs`)이 `'시트'!A1` 처럼
  **행 번호가 붙은 참조만** 잡았습니다. 그런데 원본 3p의 SUMIFS는
  `=SUMIFS('6월데이터'!$E:$E,'6월데이터'!$A:$A,"외상매출금",…)` 처럼
  **열 전체**를 참조합니다(행 번호 없음) → 이름표가 안 바뀐 채 남아
  없는 시트를 가리키게 됐습니다. 3p 44곳·연간 8곳·2-1 1곳
- 수정: 행 보정이 필요한 참조를 먼저 처리하고, **남은 이름표를 전부** 바꾸도록
  2단계로 변경 (따옴표 있는 이름·없는 이름 모두 처리)
- 검증: 산출 파일을 LibreOffice로 재계산 → **오류 0건**,
  7월 실적 기말 `321.357313` 재현

### 시트 순서 지정
- `요약1 · 요약2 · 유동자금세부 · 연간 · 실적월 · 계획월 · 2025년 자금실적 · 2026년 자금실적`
  (`SHEET_ORDER` 상수. 목록에 없는 시트는 뒤에 원래 순서대로)

### 회귀 테스트
- **없는 시트를 가리키는 수식이 0건인지 전수 검사** 항목 추가 (이번 사고 재발 방지)
- 시트 순서 검사 추가 → `tests/test_report.py` **총 16항목**

---

## 2026-08-25

### 5 엑셀·PPT 산출 화면
- **다운로드 버튼을 눌러도 화면이 사라지지 않습니다.** 만든 결과를 `session_state`에
  남겨두고 버튼을 "파일 만들기" 블록 **밖**에서 그리도록 바꿨습니다
  (예전에는 엑셀을 받는 순간 화면이 새로 그려지며 PPT 버튼까지 통째로 사라졌습니다)
- 파일이 저장된 폴더 경로 표시 + [결과 지우기] 버튼 추가

### 엑셀 산출물

**요약1**
- 위 표(자금수지)가 E열, 아래 표(유동자금 계획)가 C열에서 시작해 어긋나 보이던 것 →
  **위 표를 두 칸 왼쪽으로 옮겨 시작 열을 맞춤**(값·서식·병합·수식 참조 모두 함께 이동).
  v0.8에서 맞춰뒀다가 8/12 코드 재작성 때 유실됐던 항목입니다
- **비고란을 원본 규칙대로 복원** — 윗줄 `● 실적월`, 아랫줄 `○ 계획월`.
  예전에는 4줄 모두 계획월로 찍혔습니다

**요약2**
- 표 사이가 46줄이나 벌어지던 것 → **요약1과 똑같이 5줄**로 맞춤.
  원인은 `max_row`(서식만 있는 빈 행까지 셈) 기준으로 간격을 잡았던 것 →
  **값이 있는 마지막 행** 기준 + 아래 표의 시작 행까지 감안하도록 변경

**연간**
- **계획월(8월) 열이 아예 안 채워지던 것** → 실적월·계획월 둘 다 채웁니다
- 채우는 행이 14개뿐이라 급여·상여·퇴직연금·4대보험·법인세·부가세·원천세·
  기타제세·전산·외주·경영용역·기타소계가 **템플릿의 옛 예상치 그대로** 남아 있었음
  → **24개 행 전부**로 매핑을 완성

**2026년 자금실적**
- 🔴 **행이 한 칸씩 밀려서 기록되고 있었습니다.** 코드가 `마케팅,지급이자,외주용역비`
  행이 19행에 있다고 전제했는데 실제 템플릿에는 그 행이 없어서, 제세공과금 값이
  전산관련지출 자리에… 통신비등 값이 **'비경상' 소계 자리(=SUM 수식을 숫자로 덮어씀)** 까지
  전부 어긋났습니다
- → **행 번호를 코드에 박지 않고 시트의 라벨 글자를 읽어 찾도록** 변경
  (사용자 결정 — 행이 추가·삭제돼도 안 깨짐, 라벨을 못 찾으면 경고)
- → **계획월 열도 채웁니다**(사용자 결정)
- → `마케팅,지급이자,외주용역비` 금액이 있는데 행이 없으면 **산출할 때 자동으로 행을 삽입**
  하고 밀린 수식을 보정합니다(원본 템플릿은 건드리지 않음, 사용자 결정)
- → **B. 수입(경상/비경상)도 반영**. 확정 규칙(자산매각대금·투자분배금·투자수익만 비경상)
  그대로 적용

- **실적월 머리글에 `(A)` 자동 부여** — 이 시트는 확정된 달만 `1월(A) … 6월(A)` 처럼
  (A)를 답니다. 실적을 채워 넣었으니 그 달도 `7월(A)`가 되도록 자동 처리
  (계획월과 그 뒤 달은 손대지 않음, 사용자 결정)
- 9~12월에 남아 있는 옛 예상치는 **그대로 둡니다**(사용자 결정)

**원시 데이터 시트**
- `6월데이터` / `7월데이터` → **`실적월` / `계획월`** 로 이름 변경(수식 참조도 함께 보정)

**유동자금세부 N열 비고**
- 거래가 없는 행에 **지난달 문구가 이번 달 이름표를 달고 남아 있던 문제** 수정
  (6월 정기예금 해지 문구가 `∙7월 : …`로 둔갑). 매달 새로 쓰기 전에 먼저 비웁니다
- 비고를 **월 표시 자동화보다 나중에** 채우도록 순서 교정 —
  먼저 채우면 비고 안의 "7월"까지 계획월로 바뀌어 버렸습니다

### 회귀 테스트
- `tests/test_report.py` 에 8항목 추가 → **총 14항목**(원본 워크북·PPT가 있을 때 전부 실행)
- `tests/run_all.py` 전체 통과

---

## 2026-08-24

### 보고서 숫자 오류 4건 수정 (미리보기·엑셀·PPT 공통)

실사용 한 바퀴 점검 중 발견. 넷 다 **에러 없이 조용히 틀린 숫자가 나오던** 문제입니다.

1. **월 표기가 연쇄로 두 번 바뀌던 것** (`core/excel_writer.py`, `core/ppt_writer.py`)
   - `"6월"→7월` 을 먼저 하고 `"7월"→8월` 을 나중에 해서, 실적 7월·계획 8월이면
     **원래 6월이던 자리까지 8월**이 됐습니다. 한 번의 정규식으로 동시 치환하도록 수정
2. **환산계를 '원화' 칸에 넣어 외화가 두 번 더해지던 것** (`core/report.py`)
   - `ra.closing`/`rp.closing` 은 이미 `원화 + 외화×환율` 인데 이걸 원화 인자로 넘기고
     다시 환산 → **같은 기말잔고가 표마다 다른 숫자**로 찍혔습니다
   - `2. 유동자금 계획`·`4. 월말 자금(stock)` 을 원화/외화 성분 그대로 쓰도록 수정.
     `G. 예치 전 기말` 은 `I + 예치` 로 계산해 **G−H=I** 가 항상 맞게 함
3. **기초 운용자금에 '당월말' 값이 들어가던 것** (`app/pages/4_Preview_Report.py`)
   - 라벨은 "전월말"인데 실제로는 이번 달 `month_balances.invest_krw`(= 당월말)를 씀
     → 기초 2,400 자리에 2,100이 들어가 기말이 1,800으로 틀어짐
   - 5번 산출 화면과 **같은 순서**로 전월값을 찾도록 통일 + 출처를 화면에 표시.
     계획 외화 기초도 함께 반영
4. **세부내역 36줄에 지급이자 행이 없는데 지출 합계엔 포함되던 것** (`core/report.py`)
   - 원본 PPT 6장을 직접 열어 확인 → **지급이자 행 없음**(3p 시트에는 25행에 있음)
   - 이 표에서만 `기타 > 기타` 에 합산하도록 수정 → 지출 소계 = 하위 항목 합

### 추가로 발견·수정
- **슬라이드 글상자의 월 표기가 한 번도 안 바뀌고 있었음** (`core/ppt_writer.py`)
  - 파워포인트가 `"7월"` 을 `"7"` / `"월"` 처럼 여러 run으로 쪼개 두는데 run 단위로만
    찾고 있어서, 원본의 월 표기 **5곳 전부** 그대로 남아 있었습니다
  - 문단 전체에서 찾되 **바뀌는 글자가 든 run만** 고치는 방식으로 교체 → 글꼴·색 유지
- **FX 거래가 있으면 경고** — `1. 자금수지`·`2. 유동자금 계획` 표에는 FX 행이 없어
  기말잔고가 세부내역과 달라 보일 수 있음을 4번 화면에 표시
- **2·3장 맨 위 요약 문장도 자동으로 채웁니다** (`core/ppt_writer.py`)
  - 예) `8월 자금수지(flow) : 3,055.0백만 (기초 2,970.0백만 + 자금수지차 +85.0백만
    = 기말 3,055.0백만)` / `4. 8월말 자금(stock) : … = 유동자금 … + 운용자금 …`
  - 표가 아니라 자유 문장이지만 숫자가 전부 `…백만` 형태라, **그 숫자만 골라** 갈아 끼웁니다
  - 문단을 통째로 덮어쓰지 않고 숫자가 든 run만 고쳐서 **글꼴·굵기·색 그대로 유지**
  - 증감 칸은 값에 맞춰 색도 다시 칠합니다 (음수 파랑 / 증가 빨강, 뒤따르는 '백만'까지)
  - 문장을 못 찾거나 숫자 개수가 다르면 **건드리지 않고 경고만** 띄웁니다

### 회귀 테스트
- `tests/test_report.py` 신설 — 위 문제를 그대로 이관한 6개 항목
  (요약 문장 항목은 `templates/자금운용계획_template.pptx` 가 있을 때만 실행)
- `tests/run_all.py` 에 등록 → **총 26개 항목 전부 통과**
  (원본 워크북을 넣고 돌려 `362.495882` / `318.8615828` 재현도 확인)

---

## 2026-08-18

### PPT 자동 생성 (M7 일부)
- 지난달 보고 PPT(6장)를 템플릿으로 쓰고 **표 안의 숫자만 갈아 끼움**
  (글꼴·색·테두리 그대로 유지)
  - 1장 표지: 제목의 연·월, 작성일
  - 2장: `1. X월 자금수지` · `2. 유동자금 계획`
  - 3장: `3. 운용자금 계획` · `4. X월말 자금(stock)`
  - 5장: 연간 자금실적 — 이번 달 수입·지출 칸
  - 6장 세부내역은 자동 채우기 대상에서 제외(화면에 안내 표시)
- 검증: 6월 실적 기준으로 원본 PPT 수치 재현
  (기초 2,488.8 → 기말 2,462.5, 유동 362.5, 운용 2,100)
- `core/ppt_writer.py` 신설 / `requirements.txt` 에 `python-pptx` 추가
- 5번 화면에서 **엑셀·PPT를 골라서 함께 생성·다운로드**

### 유동자금 계획 세부내역 — PPT 6장과 동일하게 (4번·PPT)
- 미리보기 세부내역 표를 **PPT 6장과 같은 36줄·2단 머리글** 구조로 재작성
  (구분 / N월(실적) 원화·외화·환산계(a) / N월(계획) 원화·외화·환산계(b) / b-a / 비고)
- 소계(수지차·수입·지출·인건비·제세공과금·기타·FX·조달·증감)를 원화/외화 각각 계산
- **비고 칸 추가 + 직접 수정 가능** → 저장하면 PPT 6장에 그대로 들어감
- PPT 6장도 자동 채우기 대상에 포함 (이전에는 수동 안내만 표시)

### 보고서 미리보기를 실제 출력물 모양으로 (4번)
- 표를 **PPT·엑셀과 같은 모양**으로 렌더링 (머리글 음영, 숫자 우측정렬,
  0은 `-`, 음수는 파란색, 마지막 줄 강조)
- **비고 칸을 직접 쓸 수 있고, 저장하면 PPT에 그대로 들어감**
  (`report_remarks` 테이블 신설)
- 버그: 열 이름이 중복(`외화($)`가 두 번)돼 표가 안 그려지던 문제 →
  `6월 외화($)` / `7월 외화($)` 처럼 구분
- 버그: 표 높이에 `None`을 넘겨 최신 Streamlit에서 오류나던 문제

### 보고서 전체 미리보기 (4번)
- 지금까지 요약1 비고만 미리 볼 수 있던 것을, **보고서 표 5종 전체**로 확대
  (자금수지 · 유동자금 계획 · 운용자금 계획 · 월말 자금 · 세부내역 — 탭으로 전환)
- `core/report.py` 신설 — **미리보기와 PPT가 같은 계산 결과를 공유**하므로
  화면에서 본 숫자가 그대로 PPT에 들어감

### 그 외
- **산출 파일을 `.xlsm`으로 변경** — `.xlsx`로 저장하면 엑셀이
  "매크로 제외 파일이지만 매크로로 사용 내용이 들어 있습니다" 경고를 띄우던 문제 해결.
  원본이 매크로 포함 파일이라 산출 파일도 같은 형식으로 맞춤.
  (LibreOffice 재계산 재검증 — 수식 1,742개 중 오류 0개)

---

## 2026-08-13

### 잔고 자동 인식 — 사용자 입력 제거
- 일일자금계획의 **「3. 자금현황」 표를 글자로 찾아** 당일잔액 합계를 읽음
  (행 번호가 시트마다 달라도 동작 — 126행/127행 확인)
  - `1) 원화 수시입출 예금` → 유동잔고(원화)
  - `2) 외화(USD) 수시입출 예금` → 유동잔고(외화)
  - `3) 운용자금` → 운용자금 잔액
- **기초잔고 = 전월 마지막 시트**(숨김 시트 포함, 예: `0630-1`)에서 자동 인식
- **기말잔고 = 당월 마지막 시트**에서 자동 인식 → **다음 달 기초잔고로 이어짐**
- 계산값(기초+당월 수지) vs 파일 실제값을 자동 대조. 다르면 경고 + 누락 점검 안내,
  다음 달 기초에는 실제값 사용
- `month_balances` 테이블 신설 / 4·5번 화면에 자동 반영
- 검증: 7월 파일로 기초 362.495882 → 기말 321.357313, 계산값과 **차이 0**

### 미리보기 화면 재설계 (4번)
- 실적과 계획이 기초잔고를 공유하던 문제 수정
  (원칙: 다음 달 기초 = 이번 달 기말)
- 실적/계획을 나눠 표시, 검증 게이트도 각각 실행

### 중복 데이터 문제 수정
- **원인**: 계획 저장이 기존 데이터를 안 지우고 계속 덧붙임 (원천세 중복의 원인)
- 저장 방식 선택 추가 — `갱신`(기존 삭제 후 저장) / `추가`(덧붙임)
- 3번 화면에 **중복 추적기** 신설: 부서가 다른지 같은지로 원인 구분,
  항목별·일괄·부서별 정리 버튼

### 계획 업로드 개선 (2번)
- **여러 부서 파일 한꺼번에 업로드** + 파일명에서 부서명 자동 추출
- **시트 선택** 기능 — 숨김 시트 제외, 계획 월과 이름이 맞는 시트 우선
  (인프라실: 시트 76개 중 숨김 72개)
- 시트 이름 인식 확대: `2026년 8월` `2026-08` `26.08` `202608` `2608` `8월`
  `8월계획` `계획(8월)` `Aug` `August`
- 열 지정 UI 개편: 열 번호 → **엑셀 열 이름 + 헤더 글자 + 실제 값 3개**
- 감지 결과를 표로 표시, 못 찾은 항목은 빨간색 강조
- 계정과목 확인: 우선순위 정렬 + 20건씩 페이지 + **저장 전 전체 검산**

### 속도 개선 1.20초 → 0.33초
- 원인은 알고리즘이 아니라 **파일을 여러 번 여는 것**이었음
- `workbook_overview()` 로 한 번만 열어 시트목록·숨김여부·추천시트·미리보기 일괄 취득
- Streamlit 캐시 적용 → 화면 재실행 시 추가 로딩 0초

### 태그 사전 관리 화면 신설 (7번)
- 계정과목별 적요 예시를 직접 추가·수정 (저장 즉시 반영)
- 예시가 없는 계정과목만 골라보기
- 사유를 입력해 분류 결과를 즉석에서 시험

### 화면 번호 정리
- 설계서의 P2(실적 확인)가 P4에 흡수되며 번호가 1·3·4…8로 띄엄띄엄했음 → **1~7로 재정렬**

  | 이전 | 지금 | 화면 |
  |---|---|---|
  | 1 | 1 | 실적 업로드 |
  | 3 | 2 | 계획 업로드 |
  | 4 | 3 | 실적/계획 관리 |
  | 5 | 4 | 전체 미리보기 |
  | 6 | 5 | 엑셀 산출 |
  | 7 | 6 | 비고 라벨 관리 |
  | 8 | 7 | 태그 사전 관리 |

### 테스트·문서 정비
- `tests/test_parsers.py` 신설 — 외부 파일 없이 도는 파서 회귀 테스트 15개
  (실적/계획 표 분리, 숨김 A열, 소계 경계, 합계 열, 부서명 추출, 시트 이름 10형태,
  「3. 자금현황」 행 위치 변동)
- `tests/run_all.py` — 테스트 일괄 실행
- `CHANGELOG.md` 신설 / `진행상황.md` 를 "다음 할 일" 중심으로 재작성 /
  `확인필요.md` 는 CHANGELOG에 흡수 후 삭제
- `templates/README.md` — 행 추가된 템플릿을 써야 한다는 경고 추가

### 버그 수정
- 27열 파일에서 미리보기 표가 터지던 오류 (열 12개씩 묶어 보기로 해결)
- `합 계 (외화 제외)` 같은 합계 행이 거래로 섞이던 문제
- 화면 파일명이 한글이라 압축 해제 시 깨지던 문제 → **영문 파일명으로 변경**
- 사이드바가 안 보이던 문제 → 기본 펼침 + 홈 화면에 이동 버튼 추가

---

## 2026-08-12

### 원본 워크북 대조로 계산 로직 전면 재작성
업로드받은 `카탈리스터_7월_자금운용계획_260714_.xlsm` 을 직접 열어 대조.

- **계정과목 이름을 원본 그대로로 교체** — 설계서의 34개 태그는 임의 작명이었고,
  원본 `3p` 시트 수식이 참조하는 실제 이름이 따로 있었음
  (외상매출금·광고판촉비·전산유지비·경영지원용역비·금융상품 등)
- **3p 시트 행 구조(6~49행) 그대로 반영** — 인건비 15~18, 제세공과금 21~24,
  기타 29~32, 세부 33~38, 운용자금 40~41, FX 43~44, 조달 46~47
- 시트 합치기 수식 버그 수정 — 2-2를 요약2로 옮길 때 다른 시트 참조의 행 번호까지
  같이 밀려 `#VALUE!` 12개 발생하던 문제

**검증 결과**

| 항목 | 결과 |
|---|---|
| 유동자금세부 기말잔고 (6월 실적) | 362.495882 = 원본 일치 |
| 유동자금세부 기말잔고 (7월 계획) | 318.8615828 = 원본 일치 |
| 자금실적(당해) 6월 인건비 | 19.808037 = 원본 손입력값 일치 |
| 자금실적(당해) 6월 기말잔고 | 2,462.486882 = 원본 일치 |
| 운용자금 이월 | 2,400 − 300 = 2,100 = 원본 일치 |
| LibreOffice 재계산 | 수식 1,742개 중 오류 0개 |

### 사용자 결정 반영
- `요약` 레거시 시트 → 산출 파일에서 제외
- 수입 경상/비경상 → 비경상은 자산매각대금·투자분배금·투자수익, 나머지 전부 경상
- **마케팅,지급이자,외주용역비 행 추가** (방법 A) — 템플릿 19행에 삽입,
  아래로 밀린 수식 전부 자동 보정 (`tools/add_marketing_row.py`)
- 환율을 `3p!P4` 와 데이터시트 `I1` **양쪽 모두**에 기록

### 일일자금계획 파서 신설
- 한 시트에 표가 둘(`1. 일일자금수지 실적` / `2. 일일자금수지 계획`) → 둘 다 읽음
- **숨김 A열의 계정과목**을 값으로 찾아냄
- `1)원화` `2) 외화(USD)` 구간 추적, 소계 행에서 표 끊기
- 계정과목이 빈 행(MMDA·자금대체)은 기본 제외
- 한 달치 = 그 달 MMDD 시트 **전부 합침**

### 부서 계획서 파서 신설
- 집계 금액은 **`합계`(부가세 포함)** — `금액`(공급가)이 아님
- `입/출금 구분` 열로 방향 판정

### 실적 월 기준으로 화면 통일
- "작업 대상 월" → **"실적 월"**. 계획 월은 자동으로 다음 달
- `core/period.py` 신설

### 원본 파일에서 발견한 문제
- **`3p!P4`(환율 칸)가 비어 있었음** — 외화 거래 시 환산액이 0으로 계산되는 상태.
  6·7월엔 외화 거래가 없어 드러나지 않았음. 앱이 양쪽에 다 써넣도록 처리.

---

## 그 이전 (설계 단계)

`자금운용계획_웹앱_설계명세서_v0.11` 기준으로 M1~M6 구현.
자세한 내용은 설계명세서 참고.
